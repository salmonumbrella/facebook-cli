{
  "name": "facebook-mcp-cli",
  "version": "3.0.0",
  "description": "Facebook Graph API MCP server with a Go CLI",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "test:go": "go test ./cmd/... ./internal/...",
    "test:ts": "bun test",
    "verify": "bash scripts/verify.sh",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint -c ./oxlint.config.ts src tests scripts",
    "lint:all": "oxlint -c ./oxlint.config.ts src tests scripts",
    "format": "oxfmt --ignore-path .gitignore --ignore-path .oxfmtignore",
    "format:check": "oxfmt --check --ignore-path .gitignore --ignore-path .oxfmtignore .",
    "check": "npm-run-all --parallel typecheck lint format:check"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "cli-table3": "^0.6.5",
    "csv-stringify": "^6.6.0",
    "js-yaml": "^4.1.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@nkzw/oxlint-config": "^1.0.1",
    "@types/bun": "latest",
    "@types/js-yaml": "^4.0.9",
    "@typescript/native-preview": "^7.0.0-dev.20260304.1",
    "npm-run-all2": "^8.0.4",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0"
  }
}
